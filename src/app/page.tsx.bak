import Image from "next/image";
import Link from "next/link";

export default function HomePage() {
  return (
    <div className="space-y-20">
      {/* HERO */}
      <section className="relative min-h-[78vh] w-screen left-1/2 right-1/2 -ml-[50vw] -mr-[50vw] overflow-hidden">
        <Image
          src="/images/hero.jpg"
          alt="Broadmead Boutique B&B"
          fill
          priority
          className="object-cover"
          sizes="100vw"
        />

        {/* overlay for legibility */}
        <div
          className="absolute inset-0"
          style={{
            background:
              "linear-gradient(90deg, rgba(15,31,38,0.72), rgba(15,31,38,0.28), rgba(15,31,38,0.10))",
          }}
        />

        <div className="relative z-10 mx-auto max-w-6xl px-4 py-24 md:py-32 text-white">
          <p className="kicker text-white/80">Boutique B&B · Tenby · Pembrokeshire</p>

          <h1 className="mt-5 text-5xl md:text-7xl font-semibold leading-[0.98] tracking-[-0.02em]">
            A calm,
            <br />
            characterful base
            <br />
            for the coast.
          </h1>

          <p className="mt-6 max-w-xl text-lg md:text-xl text-white/85">
            Thoughtful rooms, warm hospitality, and an easy walk to Tenby’s beaches, harbour, and restaurants.
          </p>

          <div className="mt-8 flex flex-wrap gap-4">
            <a
              href="#book"
              className="btn"
              style={{ background: "#ffffff", color: "#0f1f26", borderColor: "transparent" }}
            >
              Check availability
            </a>

            <Link
              href="#rooms"
              className="text-sm font-semibold underline underline-offset-4 text-white/90 hover:text-white"
            >
              View rooms
            </Link>
          </div>
        </div>

        {/* fade into page background */}
        <div
          className="absolute bottom-0 left-0 right-0 h-24"
          style={{ background: "linear-gradient(180deg, rgba(246,245,241,0), rgba(246,245,241,1))" }}
        />
      </section>

      {/* INTRO */}
      <section className="mx-auto max-w-6xl px-4 grid gap-10 md:grid-cols-12">
        <div className="md:col-span-4">
          <p className="kicker">The house</p>
          <h2 className="mt-3 text-2xl md:text-3xl font-semibold leading-tight">
            Boutique comfort, unhurried.
          </h2>
        </div>

        <div className="md:col-span-8 text-base md:text-lg max-w-2xl" style={{ color: "var(--muted)" }}>
          <p>
            Broadmead is a calm base with Tenby on the doorstep — peaceful to return to, easy to settle into.
          </p>
        </div>
      </section>

      {/* GALLERY BAND (FORCED SIDE-BY-SIDE ON DESKTOP) */}
      <section className="mx-auto max-w-6xl px-4 space-y-5">
        <div className="flex items-end justify-between gap-6">
          <div>
            <p className="kicker">Gallery</p>
            <h2 className="mt-3 text-2xl md:text-3xl font-semibold">A feel for the place.</h2>
          </div>
          <Link href="/gallery" className="text-sm no-underline hover:underline">
            View gallery
          </Link>
        </div>

        <div className="grid gap-4 md:grid-cols-3">
          {[
            { src: "/images/room-1.jpg", alt: "Room One" },
            { src: "/images/room-2.jpg", alt: "Room Two" },
            { src: "/images/room-3.jpg", alt: "Room Three" },
          ].map((g) => (
            <div
              key={g.src}
              className="overflow-hidden border"
              style={{ borderColor: "var(--line)", borderRadius: 18 }}
            >
              <div className="relative h-[130px] md:h-[150px]">
                <Image src={g.src} alt={g.alt} fill className="object-cover" sizes="33vw" />
              </div>
            </div>
          ))}
        </div>
      </section>

      {/* ROOMS */}
      <section id="rooms" className="mx-auto max-w-6xl px-4 space-y-7">
        <div className="flex items-end justify-between gap-4">
          <div>
            <p className="kicker">Rooms</p>
            <h2 className="mt-3 text-2xl md:text-3xl font-semibold">Choose your room.</h2>
          </div>
          <Link href="/rooms" className="text-sm no-underline hover:underline">
            See all rooms
          </Link>
        </div>

        <div className="grid gap-6 md:grid-cols-3">
          {[
            { name: "Room One", note: "Light, calm, and comfortable.", img: "/images/room-1.jpg" },
            { name: "Room Two", note: "A characterful room with thoughtful touches.", img: "/images/room-2.jpg" },
            { name: "Room Three", note: "Ideal for a slower stay.", img: "/images/room-3.jpg" },
          ].map((r) => (
            <article
              key={r.name}
              className="overflow-hidden border"
              style={{ borderColor: "var(--line)", borderRadius: 20, background: "rgba(255,255,255,0.55)" }}
            >
              <div className="relative h-[180px]">
                <Image src={r.img} alt={r.name} fill className="object-cover" sizes="33vw" />
              </div>
              <div className="px-6 py-5">
                <h3 className="text-lg font-semibold">{r.name}</h3>
                <p className="mt-2 text-sm" style={{ color: "var(--muted)" }}>
                  {r.note}
                </p>
              </div>
            </article>
          ))}
        </div>
      </section>

      {/* BOOKING CTA */}
      <section
        id="book"
        className="mx-auto max-w-6xl px-4 border p-8 md:p-10"
        style={{ borderColor: "var(--line)", borderRadius: 22, background: "rgba(255,255,255,0.65)" }}
      >
        <div className="grid gap-6 md:grid-cols-12 md:items-center">
          <div className="md:col-span-8 space-y-2">
            <p className="kicker">Book direct</p>
            <h2 className="text-2xl md:text-3xl font-semibold">Availability & rates.</h2>
            <p className="text-sm md:text-base" style={{ color: "var(--muted)" }}>
              Quick link to your live booking page.
            </p>
          </div>
          <div className="md:col-span-4 md:flex md:justify-end">
            <a href="https://broadmeadtenby.wales/availability-tariff" className="btn btn-primary">
              Availability & Rates
            </a>
          </div>
        </div>
      </section>
    </div>
  );
}
